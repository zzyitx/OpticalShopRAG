# 生产配置

<cite>
**本文档中引用的文件**  
- [application-docker.yml](file://src/main/resources/application-docker.yml)
- [vite.config.ts](file://frontend/vite.config.ts)
- [SecurityConfig.java](file://src/main/java/com/yizhaoqi/smartpai/config/SecurityConfig.java)
- [JwtUtils.java](file://src/main/java/com/yizhaoqi/smartpai/utils/JwtUtils.java)
- [TokenCacheService.java](file://src/main/java/com/yizhaoqi/smartpai/service/TokenCacheService.java)
- [KafkaConfig.java](file://src/main/java/com/yizhaoqi/smartpai/config/KafkaConfig.java)
- [MinioConfig.java](file://src/main/java/com/yizhaoqi/smartpai/config/MinioConfig.java)
- [DeepSeekClient.java](file://src/main/java/com/yizhaoqi/smartpai/client/DeepSeekClient.java)
</cite>

## 目录
1. [生产环境配置解析](#生产环境配置解析)
2. [前端构建优化配置](#前端构建优化配置)
3. [安全相关配置](#安全相关配置)
4. [配置调优建议](#配置调优建议)

## 生产环境配置解析

### 数据库连接池配置
在 `application-docker.yml` 文件中，数据库连接配置如下：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/PaiSmart?useSSL=false&serverTimezone=UTC
    username: root
    password: PaiSmart2025
    driver-class-name: com.mysql.cj.jdbc.Driver
```

该配置指定了MySQL数据库的连接信息，包括：
- **数据库URL**: `jdbc:mysql://localhost:3306/PaiSmart`，使用UTC时区
- **用户名**: `root`
- **密码**: `PaiSmart2025`
- **驱动类名**: `com.mysql.cj.jdbc.Driver`

**注意**: 该配置文件中未显式配置HikariCP连接池参数（如最大连接数、最小空闲连接、连接超时等），将使用Spring Boot的默认连接池配置。

**Section sources**
- [application-docker.yml](file://src/main/resources/application-docker.yml)

### Redis缓存策略
Redis配置如下：

```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: PaiSmart2025
```

Redis用于缓存JWT令牌状态，通过 `TokenCacheService` 类实现：

```java
public void cacheToken(String tokenId, String userId, String username, long expireTimeMs) {
    try {
        String key = TOKEN_PREFIX + tokenId;
        Map<String, Object> tokenInfo = new HashMap<>();
        tokenInfo.put("userId", userId);
        tokenInfo.put("username", username);
        tokenInfo.put("expireTime", expireTimeMs);
        
        // 计算Redis过期时间（比JWT过期时间稍长一点）
        long ttlSeconds = (expireTimeMs - System.currentTimeMillis()) / 1000 + 300; // 多5分钟缓冲
        
        redisTemplate.opsForValue().set(key, tokenInfo, ttlSeconds, TimeUnit.SECONDS);
    } catch (Exception e) {
        logger.error("Failed to cache token: {}", tokenId, e);
    }
}
```

缓存策略特点：
- **键前缀**: 使用 `jwt:valid:` 作为有效令牌的键前缀
- **过期时间**: Redis中的过期时间比JWT令牌的过期时间多5分钟缓冲
- **数据结构**: 使用哈希结构存储令牌信息，包括用户ID、用户名和过期时间

**Section sources**
- [application-docker.yml](file://src/main/resources/application-docker.yml)
- [TokenCacheService.java](file://src/main/java/com/yizhaoqi/smartpai/service/TokenCacheService.java)

### Kafka消息队列配置
Kafka配置如下：

```yaml
spring:
  kafka:
    enabled: true
    bootstrap-servers: 127.0.0.1:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        client.dns.lookup: use_all_dns_ips
    consumer:
      group-id: file-processing-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
        client.dns.lookup: use_all_dns_ips
    topic:
      file-processing: file-processing-topic1
```

Kafka配置特点：
- **生产者**: 使用JSON序列化，配置了DNS查找策略
- **消费者**: 消费者组ID为 `file-processing-group`，自动偏移重置策略为 `earliest`
- **主题**: 文件处理主题为 `file-processing-topic1`
- **安全**: 允许反序列化所有包（`*`）

在 `KafkaConfig` 类中，还配置了可靠的生产者和消费者工厂：

```java
@Bean
public ProducerFactory<String, Object> producerFactory() {
    Map<String, Object> config = new HashMap<>();
    config.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, bootstrapServers);
    config.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, StringSerializer.class);
    config.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, JsonSerializer.class);
    // 可靠投递配置
    config.put(ProducerConfig.ACKS_CONFIG, "all"); // 全部 ISR 落盘才确认
    config.put(ProducerConfig.ENABLE_IDEMPOTENCE_CONFIG, true); // 幂等生产者
    config.put(ProducerConfig.RETRIES_CONFIG, 3); // 自动重试 3 次
    // ...
}
```

**Section sources**
- [application-docker.yml](file://src/main/resources/application-docker.yml)
- [KafkaConfig.java](file://src/main/java/com/yizhaoqi/smartpai/config/KafkaConfig.java)

### MinIO文件存储配置
MinIO配置如下：

```yaml
minio:
  endpoint: http://localhost:19000
  accessKey: aHZtaNPEzLcERm9uCY2F
  secretKey: 5lhyVdLKfMfYXtmdVsulLYpgCRx5EO1EfIWAtJdq
  bucketName: uploads
  publicUrl: http://localhost:19000
```

配置说明：
- **端点**: `http://localhost:19000`
- **访问密钥**: `aHZtaNPEzLcERm9uCY2F`
- **秘密密钥**: `5lhyVdLKfMfYXtmdVsulLYpgCRx5EO1EfIWAtJdq`
- **存储桶名称**: `uploads`
- **公共URL**: `http://localhost:19000`

在 `MinioConfig` 类中，使用这些配置创建 `MinioClient` 实例：

```java
@Bean
public MinioClient minioClient() {
    return MinioClient.builder()
            .endpoint(endpoint)
            .credentials(accessKey, secretKey)
            .build();
}
```

**Section sources**
- [application-docker.yml](file://src/main/resources/application-docker.yml)
- [MinioConfig.java](file://src/main/java/com/yizhaoqi/smartpai/config/MinioConfig.java)

### AI服务API密钥管理
AI服务API密钥配置如下：

```yaml
deepseek:
  api:
    url: https://api.deepseek.com/v1
    key: sk-cfd92d6071d246f9a4c8f941a735c271
    model: deepseek-chat
embedding:
  api:
    url: https://ark.cn-beijing.volces.com/api/v3
    key: b22d5f8a-a3cf-4c95-bb74-e05c45211474
    model: doubao-embedding-text-240515
```

在 `DeepSeekClient` 类中，通过Spring的 `@Value` 注解注入API密钥：

```java
public DeepSeekClient(@Value("${deepseek.api.url}") String apiUrl,
                     @Value("${deepseek.api.key}") String apiKey,
                     @Value("${deepseek.api.model}") String model,
                     AiProperties aiProperties) {
    WebClient.Builder builder = WebClient.builder().baseUrl(apiUrl);
    
    // 只有当 API key 不为空时才添加 Authorization header
    if (apiKey != null && !apiKey.trim().isEmpty()) {
        builder.defaultHeader(HttpHeaders.AUTHORIZATION, "Bearer " + apiKey);
    }
    
    this.webClient = builder.build();
    this.apiKey = apiKey;
    this.model = model;
    this.aiProperties = aiProperties;
}
```

API密钥通过配置文件直接存储，建议在生产环境中使用环境变量或密钥管理服务来提高安全性。

**Section sources**
- [application-docker.yml](file://src/main/resources/application-docker.yml)
- [DeepSeekClient.java](file://src/main/java/com/yizhaoqi/smartpai/client/DeepSeekClient.java)

## 前端构建优化配置

### 生产优化选项
在 `vite.config.ts` 文件中，前端构建配置如下：

```typescript
export default defineConfig(configEnv => {
  const viteEnv = loadEnv(configEnv.mode, process.cwd()) as unknown as Env.ImportMeta;

  const buildTime = getBuildTime();

  const enableProxy = configEnv.command === 'serve' && !configEnv.isPreview;

  return {
    base: viteEnv.VITE_BASE_URL,
    resolve: {
      alias: {
        '~': fileURLToPath(new URL('./', import.meta.url)),
        '@': fileURLToPath(new URL('./src', import.meta.url))
      }
    },
    css: {
      preprocessorOptions: {
        scss: {
          api: 'modern-compiler',
          additionalData: `@use "@/styles/scss/global.scss" as *;`
        }
      }
    },
    plugins: setupVitePlugins(viteEnv, buildTime),
    define: {
      BUILD_TIME: JSON.stringify(buildTime)
    },
    server: {
      host: '0.0.0.0',
      port: 9527,
      open: true,
      proxy: createViteProxy(viteEnv, enableProxy),
      allowedHosts: ['u45964x883.zicp.vip']
    },
    preview: {
      port: 9725
    },
    build: {
      reportCompressedSize: false,
      sourcemap: viteEnv.VITE_SOURCE_MAP === 'Y',
      commonjsOptions: {
        ignoreTryCatch: false
      }
    }
  };
});
```

关键优化配置：
- **代码压缩**: Vite默认使用Rollup进行代码压缩，但 `reportCompressedSize` 设置为 `false`，不报告压缩后大小
- **资源哈希**: Vite默认为静态资源生成哈希文件名，实现缓存失效
- **CDN支持**: 通过 `base` 配置项支持CDN，其值来自环境变量 `VITE_BASE_URL`
- **Source Map**: 根据环境变量 `VITE_SOURCE_MAP` 决定是否生成source map
- **构建时间**: 将构建时间注入到全局变量 `BUILD_TIME` 中，便于追踪

**Section sources**
- [vite.config.ts](file://frontend/vite.config.ts)

## 安全相关配置

### JWT令牌配置
在 `JwtUtils.java` 文件中，JWT令牌配置如下：

```java
private static final long EXPIRATION_TIME = 3600000; // 1 hour (调整为1小时)
private static final long REFRESH_TOKEN_EXPIRATION_TIME = 604800000; // 7 days (refresh token有效期)
private static final long REFRESH_THRESHOLD = 300000; // 5分钟：当剩余时间少于5分钟时开始刷新
private static final long REFRESH_WINDOW = 600000; // 10分钟：token过期后的宽限期
```

JWT配置说明：
- **访问令牌有效期**: 1小时
- **刷新令牌有效期**: 7天
- **刷新阈值**: 当令牌剩余时间少于5分钟时开始刷新
- **刷新窗口**: 令牌过期后10分钟内仍可刷新

在 `SecurityConfig.java` 中，配置了无状态会话管理：

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    @Autowired
    private JwtAuthenticationFilter jwtAuthenticationFilter;
    
    @Autowired
    private OrgTagAuthorizationFilter orgTagAuthorizationFilter;

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        try {
            http
                .csrf(csrf -> csrf.disable())
                .cors(cors -> cors.configurationSource(corsConfigurationSource()))
                .authorizeHttpRequests(auth -> auth
                    .requestMatchers("/api/auth/**").permitAll()
                    .requestMatchers("/api/public/**").permitAll()
                    .requestMatchers(HttpMethod.GET, "/api/parse/**").hasAnyRole("USER", "ADMIN")
                    .requestMatchers("/api/admin/**").hasRole("ADMIN")
                    .anyRequest().authenticated())
                .sessionManagement(session -> session
                    .sessionCreationPolicy(SessionCreationPolicy.STATELESS))
                .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class)
                .addFilterAfter(orgTagAuthorizationFilter, JwtAuthenticationFilter.class);

            logger.info("Security configuration loaded successfully.");
            return http.build();
        } catch (Exception e) {
            logger.error("Failed to configure security filter chain", e);
            throw e;
        }
    }
}
```

安全配置特点：
- **无CSRF**: 禁用CSRF保护，因为使用JWT进行身份验证
- **CORS**: 启用CORS并配置自定义CORS配置源
- **无状态会话**: 使用 `STATELESS` 会话创建策略
- **JWT过滤器**: 在用户名密码认证过滤器之前添加JWT认证过滤器

**Section sources**
- [JwtUtils.java](file://src/main/java/com/yizhaoqi/smartpai/utils/JwtUtils.java)
- [SecurityConfig.java](file://src/main/java/com/yizhaoqi/smartpai/config/SecurityConfig.java)

### 敏感信息加密
敏感信息如数据库密码、Redis密码、MinIO密钥等直接存储在 `application-docker.yml` 配置文件中。建议在生产环境中使用以下方法提高安全性：

1. **环境变量**: 将敏感信息通过环境变量注入
2. **密钥管理服务**: 使用AWS KMS、Hashicorp Vault等密钥管理服务
3. **配置中心**: 使用Spring Cloud Config等配置中心，对敏感信息进行加密存储

**Section sources**
- [application-docker.yml](file://src/main/resources/application-docker.yml)

## 配置调优建议

### 数据库连接池调优
建议显式配置HikariCP连接池参数以适应不同生产负载：

```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
```

根据负载情况调整：
- **低负载**: 最大连接数5-10，最小空闲连接2-3
- **中等负载**: 最大连接数15-25，最小空闲连接5-8
- **高负载**: 最大连接数30-50，最小空闲连接10-15

### Redis缓存调优
建议根据实际使用情况调整Redis缓存策略：
- **增加缓存过期时间**: 对于不经常变化的数据，可适当增加过期时间
- **使用Redis集群**: 在高并发场景下，使用Redis集群提高可用性和性能
- **监控缓存命中率**: 定期监控缓存命中率，优化缓存策略

### Kafka调优
生产环境建议调整Kafka配置：
```yaml
spring:
  kafka:
    producer:
      acks: all
      retries: 10
      enable-idempotence: true
      transactional-id-prefix: file-upload-tx-
      properties:
        request.timeout.ms: 30000
        retry.backoff.ms: 1000
    consumer:
      max-poll-records: 500
      fetch-max-wait: 500
      fetch-min-size: 1
      properties:
        max.poll.interval.ms: 300000
```

### 前端构建调优
建议在 `vite.config.ts` 中增加以下生产优化配置：

```typescript
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        'vue': ['vue', 'vue-router'],
        'naive-ui': ['naive-ui'],
        'axios': ['axios'],
      }
    }
  },
  chunkSizeWarningLimit: 1000,
  cssCodeSplit: true,
  minify: 'terser',
  terserOptions: {
    compress: {
      drop_console: true,
      drop_debugger: true
    }
  }
}
```

这些优化包括：
- **代码分割**: 手动分割常用库，提高缓存利用率
- **压缩**: 使用terser进行更高级的压缩，移除console和debugger语句
- **CSS分割**: 启用CSS代码分割
- **警告限制**: 提高代码块大小警告限制

**Section sources**
- [application-docker.yml](file://src/main/resources/application-docker.yml)
- [vite.config.ts](file://frontend/vite.config.ts)